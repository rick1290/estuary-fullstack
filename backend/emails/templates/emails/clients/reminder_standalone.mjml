<mjml>
  <mj-head>
    <mj-font name="DM Sans" href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&display=swap" />
    <mj-attributes>
      <mj-all font-family="'DM Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif" />
      <mj-text font-size="16px" line-height="1.6" color="#3c412a" />
      <mj-section background-color="#fdfcf8" padding="20px 0" />
      <mj-button background-color="#7a8b63" color="#ffffff" font-weight="600" border-radius="12px" padding="14px 32px" font-size="16px" />
    </mj-attributes>
  </mj-head>

  <mj-body background-color="#f5f1e6">
    <!-- Header -->
    <mj-section background-color="#ffffff" padding="32px 24px">
      <mj-column>
        <mj-text align="center" font-size="20px" font-weight="700" color="#3c412a" letter-spacing="5px" padding="12px 0">
          ESTUARY
        </mj-text>
      </mj-column>
    </mj-section>

    <!-- Content -->
    <mj-wrapper background-color="#fdfcf8" padding="0 24px">
      <!-- Reminder Badge -->
      <mj-section padding="12px 0 8px">
        <mj-column>
          <mj-text align="center" font-size="18px" padding-bottom="4px">
            🔔
          </mj-text>
          <mj-text align="center" font-size="18px" font-weight="700" color="#3c412a" padding-bottom="2px">
            Session Tomorrow
          </mj-text>
          <mj-text align="center" font-size="13px" color="#6f7a47">
            Your session starts in 24 hours
          </mj-text>
        </mj-column>
      </mj-section>

      <!-- Session Details -->
      <mj-section background-color="#ffffff" border-radius="16px" padding="24px 20px">
        <mj-column>
          <!-- Service Name -->
          <mj-text font-size="18px" font-weight="600" color="#7a8b63" padding-bottom="12px">
            {{ service_name }}
          </mj-text>

          <mj-text padding-bottom="12px">
            <span style="font-weight: 600; color: #3c412a;">👤 With:</span> {{ practitioner_name }}
          </mj-text>

          <mj-text padding-bottom="12px">
            <span style="font-weight: 600; color: #3c412a;">📅 Date:</span> {{ booking_date }}
          </mj-text>

          <mj-text padding-bottom="20px">
            <span style="font-weight: 600; color: #3c412a;">⏰ Time:</span> {{ booking_time }} ({{ duration_minutes }} min)
          </mj-text>

          <!-- CTA Button -->
          {% if has_video_room %}
          <mj-button href="{{ join_url }}">
            Join Session
          </mj-button>
          {% else %}
          <mj-button href="{{ booking_url }}">
            View Session Details
          </mj-button>
          {% endif %}
        </mj-column>
      </mj-section>

      <!-- Preparation Tip -->
      <mj-section background-color="#f5f1e6" border-radius="12px" border-left="4px solid #7a8b63" padding="24px 28px">
        <mj-column>
          <mj-text font-size="14px" font-weight="600" color="#6f7a47" padding-bottom="8px">
            💡 Preparation Tip
          </mj-text>
          <mj-text font-size="14px" line-height="1.6" color="#3c412a">
            Find a quiet space and have your camera and microphone ready. You'll receive another reminder 30 minutes before the session starts.
          </mj-text>
        </mj-column>
      </mj-section>

      <!-- Quick Actions -->
      <mj-section padding="32px 0">
        <mj-column>
          <mj-text align="center" font-size="14px" color="#8b985b">
            Need to reschedule? <a href="{{ WEBSITE_URL }}{{ URL_PATHS.USER_BOOKINGS }}" style="color: #7a8b63; font-weight: 600;">Manage Booking →</a>
          </mj-text>
        </mj-column>
      </mj-section>
    </mj-wrapper>

    <!-- Footer -->
    <mj-section background-color="#3c412a" padding="40px 24px">
      <mj-column>
        <mj-text align="center" color="#ffffff" font-size="18px" font-weight="700" letter-spacing="5px" padding-bottom="16px">
          ESTUARY
        </mj-text>
        <mj-text align="center" color="#95a57f" font-size="14px" padding-bottom="24px">
          Your journey to wellness starts here
        </mj-text>
        <mj-text align="center" color="#8b985b" font-size="13px" padding-bottom="16px">
          <a href="{{ WEBSITE_URL }}{{ URL_PATHS.ABOUT }}" style="color: #7a8b63;">About</a> ·
          <a href="{{ WEBSITE_URL }}{{ URL_PATHS.HELP }}" style="color: #7a8b63;">Help</a> ·
          <a href="{{ WEBSITE_URL }}{{ URL_PATHS.PRIVACY }}" style="color: #7a8b63;">Privacy</a>
        </mj-text>
        <mj-text align="center" color="#8b985b" font-size="12px">
          © {{ YEAR }} ESTUARY. All rights reserved.
        </mj-text>
      </mj-column>
    </mj-section>
  </mj-body>
</mjml>
